<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <style>
      {% include "style.css" %}
      {% if templateClass == "tmpl-article" %}
        {% include "prism.css" %}
      {% endif %}
    </style>
  </head>
  <body>
  <div class="container">
    <header>
      <a href="{{ '/' | url }}">blog.soyoil.net</a>
    </header>
    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
      {{ content | safe }}
    </main>
    </div>
  </body>
</html>
